# Main playground for Java Micro-benchmark harness


# Installation
* `java 17` used
* `jenv` used for correct java version
* Current project uses maven wrapper (was installed with `mvn wrapper:wrapper -Dmaven=3.8.6`)
